;;; =====================================================================
;;; main.sot - Код приложения
;;; =====================================================================

(with-segment main
 (defblock main
  (zasm
    (.label 'main)
    (.nop)
    (.halt)))
    
) ;; end of segment
    
